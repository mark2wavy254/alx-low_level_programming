#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the int to check
 * Rteurn: the absolute value of int
 */

int_abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

